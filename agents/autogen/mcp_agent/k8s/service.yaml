apiVersion: v1
kind: Service
metadata:
  name: autogen-agent
  labels:
    app: autogen-agent
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: autogen-agent
