apiVersion: v1
kind: Service
metadata:
  name: mcp-automl
  labels:
    app: mcp-automl
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mcp-automl
