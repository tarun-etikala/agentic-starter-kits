# Helm values override for LlamaIndex WebSearch Agent
nameOverride: llamaindex-websearch-agent

resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"
