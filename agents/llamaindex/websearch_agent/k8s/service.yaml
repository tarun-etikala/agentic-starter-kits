apiVersion: v1
kind: Service
metadata:
  name: llamaindex-websearch-agent
  labels:
    app: llamaindex-websearch-agent
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: llamaindex-websearch-agent
