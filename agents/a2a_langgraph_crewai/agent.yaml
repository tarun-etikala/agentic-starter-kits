name: a2a-langgraph-crewai
displayName: "A2A LangGraph ↔ CrewAI"
framework: a2a
description: "A2A demo — CrewAI specialist and LangGraph orchestrator (two images, one Dockerfile)."
env:
  required:
    - API_KEY
    - BASE_URL
    - MODEL_ID
  optional:
    - CONTAINER_IMAGE
    - CONTAINER_IMAGE_CREW
    - CONTAINER_IMAGE_LANGGRAPH
