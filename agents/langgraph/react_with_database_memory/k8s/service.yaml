apiVersion: v1
kind: Service
metadata:
  name: langgraph-db-memory
  labels:
    app: langgraph-db-memory
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: langgraph-db-memory
