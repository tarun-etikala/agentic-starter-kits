apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: langgraph-hitl-agent
  labels:
    app: langgraph-hitl-agent
spec:
  to:
    kind: Service
    name: langgraph-hitl-agent
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
