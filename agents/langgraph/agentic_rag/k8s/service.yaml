apiVersion: v1
kind: Service
metadata:
  name: langgraph-agentic-rag
  labels:
    app: langgraph-agentic-rag
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: langgraph-agentic-rag