apiVersion: v1
kind: Service
metadata:
  name: crewai-websearch-agent
  labels:
    app: crewai-websearch-agent
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: crewai-websearch-agent
