apiVersion: v1
kind: Service
metadata:
  name: openai-responses-agent
  labels:
    app: openai-responses-agent
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: openai-responses-agent
